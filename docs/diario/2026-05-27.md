# Diário de Sessão — 2026-05-27

## O que foi feito

### 1. Recuperação de senha (concluído)
- Implementado fluxo completo: `Login.tsx` (view "forgot" + "sent"), `ResetPassword.tsx` (nova página pública em `/redefinir-senha`), `AuthContext.tsx` (`resetPasswordForEmail` + `updatePassword`), `App.tsx` (rota pública antes do catch-all).
- Testado em produção: funcionou.

### 2. Bugs de encadeamento de tarefas (concluídos)
- **Bug raiz 1**: picker no `CreateTaskModal` mostrava tarefas no meio de cadeias, permitindo criar cadeias paralelas. Fix: filtrar candidatos para `(t.chains?.length ?? 0) === 0` (tarefas sem cadeia) + chain tails existentes.
- **Bug raiz 2**: endpoint `unlink_task` retornava 204 sem corpo → título antigo ficava no cache até o refetch. Fix: mudar para 200 retornando `TaskRead`, frontend usa `setQueryData` para atualização imediata.
- **Bug raiz 3**: N+1 queries — `list_today` fazia 1 query por tarefa para carregar `chains`. Fix: `chain_infos_bulk()` no `ChainService` usando 2 queries para todas as tarefas.

### 3. Tarefas invisíveis `is_pending_review` (diagnosticado e corrigido)
- 3 tarefas ("Aplicação de tracer 1/3", "2/3") sumidas porque foram importadas do Google Tasks com `is_pending_review=True`. `list_today` excluía essas tarefas silenciosamente. Feature de revisão foi descartada há muito tempo no UX mas o filtro permaneceu no código.
- Fix aplicado: SQL `UPDATE task SET is_pending_review = false WHERE is_pending_review = true`, remoção do filtro em `services/tasks.py`, `google_sync.py` alterado para `is_pending_review=False`.

### 4. Dados sumindo a cada deploy (diagnosticado, pendente fix do usuário)
- Root cause: `DATABASE_URL` não configurada no Render → backend cai no fallback SQLite → dados perdidos a cada restart.
- Fix: usuário precisa adicionar `DATABASE_URL` no dashboard do Render com o valor de `SUPABASE_DATABASE_URL` do `.env` local.

## Pendências críticas

| Item | Prioridade | Status |
|---|---|---|
| Configurar `DATABASE_URL` no Render | 🔴 CRÍTICO | Aguardando usuário |
| Revogar chave Google OAuth `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` exposta no commit `1a7192e` | 🔴 SEGURANÇA | Aguardando usuário |
| SQL limpeza final das chains corrompidas | 🟡 MÉDIO | Possivelmente feito |
| Análise de drift blueprint vs. implementação | 🟡 MÉDIO | Apresentada nesta sessão |

## Decisões registradas

- **2026-05-13**: Undo de conclusão sem janela de tempo (removida restrição de 5 min do ADR-002). Registrado em `tasks.py:133`.
- **Encadeamento**: feature adicionada organicamente (não estava no blueprint), teve múltiplos bugs de correção. Avaliar se continua ou é removida.
- **is_pending_review**: feature descartada — remover completamente do código é a próxima refatoração prioritária.

## Análise de instabilidade apresentada ao usuário

Ver análise completa no chat. TL;DR: não é problema de stack, é configuração do Render sem `DATABASE_URL`. Fix é configurar 6 variáveis de ambiente no dashboard e adicionar UptimeRobot gratuito.
