# Diário de sessão — 2026-05-19

## O que foi feito

### Revisão completa do blueprint

Sessão de auditoria do projeto: verificar inconsistências entre ADRs/PRDs e o código em execução, e corrigir desconexões no grafo do Obsidian (muitos arquivos sem nenhum link).

---

### Inconsistências corrigidas — docs vs. código

**feat-agenda.md**
- `financial_score` na seção "Not Doing" era descrito como fixo em 0 — corrigido para 0–5 manual conforme campo no modelo
- AC-7 reescrito: inclui o `CompleteModal` com textarea opcional, Ctrl+Enter confirma, e geração automática de entrada no caderno de campo
- AC-8b adicionado: reabertura de tarefa concluída sempre permitida via `POST /tasks/{id}/restore` (sem restrição de tempo)

**feat-caderno-de-campo.md**
- AC-6 atualizado para incluir campo `observation` opcional do `CompleteModal`

**feat-google-tasks-sync.md**
- Sync instantâneo via `BackgroundTasks` (~1s) documentado corretamente — o documento dizia "até 2 minutos" / "polling como mecanismo principal"
- Lista "memória" removida do escopo; todas as tarefas vão para lista única "AgroecologIA"
- AC-5 marcado como removido; AC-6, AC-8, AC-15 corrigidos

**feat-compras.md**
- 4 referências a `feat-google-calendar-tasks-sync` corrigidas para `feat-google-tasks-sync`

**adr-002-sync-offline.md**
- Janela de undo de 5 minutos documentada como removida em 2026-05-13
- `completion_locked` documentado como vestigial (sempre false, mantido por compat de schema)
- Referência a SSE atualizada para decisão WebSocket do ADR-010

**adr-010-implementacao-sync-offline.md**
- Aviso adicionado no topo: campos no schema implementados; operation log, WebSocket, Dexie, `/api/sync` ainda não implementados — produto opera online-first
- Seção "Lock de conclusão" reescrita para refletir remoção da janela de 5 min

**CLAUDE.md**
- Seção Tech Stack reescrita distinguindo implementado vs. planejado
- Seção Architecture atualizada com estrutura real de diretórios
- Seção Relacionados adicionada com wikilinks para vision, todos os PRDs e ADRs

**docs/specs/api/README.md**
- Nota "migração pendente" substituída por "concluída em 2026-05-15"

---

### Wikilinks adicionados — grafo Obsidian reconectado

Arquivos sem nenhuma conexão no grafo foram ligados ao restante da documentação:

- **vision.md** — seção Relacionados + URLs de produção + links para todos os PRDs e 4 ADRs centrais
- **Diários** (8 arquivos: 2026-05-08 a 2026-05-18) — cada um recebeu seção Relacionados com links para PRDs/ADRs relevantes à sessão e diários adjacentes
- **PRDs**: feat-onboarding, feat-mapa, feat-vendas, feat-financeiro, feat-manutencao, feat-automacao, feat-google-tasks-sync — links a ADRs dependentes
- **ADRs**: adr-005, adr-012, adr-013, adr-014 — links a feat-* dependentes

---

### Achado de segurança

`docs/diario/Anotações.md` continha uma chave OAuth do Google (`GOCSPX-...`) commitada no repositório.  
→ Chave foi exposta em contexto do repositório git. Produtor confirmou remoção.  
**Ação recomendada:** verificar se a chave foi revogada no Google Cloud Console e se havia algum commit no histórico git além do arquivo local.

---

## Pendências abertas (herdadas de 2026-05-18)

- **Novas dimensões de TAG**: Ambiente (SAF Piloto, SAF 1, SAF 2, Fotovoltaico, Reservatório) e Lote — backend models + CRUD + seed
- **Campos no modelo Task**: `activity_type_slug`, `culture_slug`, `ambiente_slug`, `lote_slug`
- **FieldNote**: adicionar os 4 campos de dimensão para rastreabilidade/filtros
- **Página de configurações unificada** `/configuracoes`: CRUD completo + seção Google integration
- **CreateTaskModal**: suporte às 4 dimensões de tag
- **Caderno de campo — filtros**: filtrar por todas as 4 dimensões
- **feat-culturas.md**: documentar Ambiente como conceito separado de Cultura
- **Deploy**: mudanças das sessões 2026-05-15 e 2026-05-18 são locais, precisam de commit + push + redeploy no Render
- **Lucide React**: `pnpm add lucide-react` e migrar SVGs inline
- **Chave OAuth exposta**: confirmar revogação no Google Cloud Console + checar histórico git

## Relacionados
- [[feat-agenda]] — AC-7 e AC-8b corrigidos
- [[feat-caderno-de-campo]] — AC-6 corrigido
- [[feat-google-tasks-sync]] — sync instantâneo e lista única documentados
- [[adr-002-sync-offline]] — janela de undo e completion_locked corrigidos
- [[adr-010-implementacao-sync-offline]] — status de implementação corrigido
- [[vision]] — wikilinks adicionados
- [[2026-05-18]] — sessão anterior (observação ao concluir + redesign UI)
